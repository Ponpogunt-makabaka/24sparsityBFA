# R1_T05 vs R1_T06.1 Global Reanalysis

**Generated**: 2026-02-23
**Purpose**: Compare T05 (joint attack) with T06.1 global (dense-format weight-bit attack)

---

## Executive Summary

**Key Finding**: T05 and T06.1 global are NOT expected to be identical due to multiple protocol differences

| Aspect | T05 | T06.1 Global |
|--------|-----|--------------|
| Attack type | Joint (weight + metadata) | Weight-bit only |
| Final acc | 9.96% | 10.20% |
| Selection method | Joint top-K across 3 types | Weight-bit top-K only |
| Exact verification | Yes | Yes |
| Metadata modified | No (all 50 were weight) | N/A |

**Conclusion**: T05 effectively becomes a weight-bit attack, with results matching T06.1 global (9.96% vs 10.20%).

---

## T05 Results Summary

### Configuration
```
seed: 0
physical_budget: 50
top_k: 64
calib_samples: 256
eval_samples: 2000
enabled_actions: weight_bit=True, index_1bit=True, bitmask_swap=True
```

### Action Breakdown
```
weight_bit: 50
index_1bit: 0
bitmask_swap: 0
```

**Critical Observation**: T05 chose **100% weight_bit** actions, despite metadata being available.

### Results
```
baseline_acc: 92.33%
final_acc: 9.96%
acc_drop: 82.37%
```

---

## T06.1 Global Results Summary

### Configuration
```
seed: 0
physical_budget: 50
top_k: 64
calib_samples: 256
eval_samples: 2000
mode: global (all weights)
```

### Results
```
sparse-gated : global weight-bit flip (dense-view search) (K=64)
  Baseline: 92.40% acc, 0.269775 loss
  Final: 10.20% acc, 17.575377 loss
  Acc increase steps: []
  Loss decrease steps: []
```

---

## Protocol Differences Analysis

### 1. Attack Surface

| Aspect | T05 | T06.1 Global |
|--------|-----|--------------|
| Candidate types | 3 (weight_bit, index_1bit, bitmask_swap) | 1 (weight_bit) |
| Actual choice | weight_bit only | weight_bit only |
| Metadata modified | No | N/A |

**Impact**: T05 had the OPTION to attack metadata but never chose it. This suggests weight-bit is consistently more damaging.

### 2. Candidate Pool Definition

| Aspect | T05 | T06.1 Global |
|--------|-----|--------------|
| Weight candidates | All weights | All weights |
| Metadata candidates | All groups (position/bitmask) | N/A |
| Per-layer top-K | Yes (implied) | Yes (per-layer \|grad\|) |

**Status**: **SIMILAR** - Both search global weight space

### 3. Stage-A (Proxy Screening)

| Aspect | T05 | T06.1 Global |
|--------|-----|--------------|
| Proxy formula | `w_fp * (g_new - g_old)` | Per-layer \|grad\| ranking |
| Top-K screening | Yes (K=64) | Yes (K=64) |

**Status**: **DIFFERENT** - T05 uses Taylor approximation; T06.1 uses gradient magnitude

### 4. Stage-B (Exact Verification)

| Aspect | T05 | T06.1 Global |
|--------|-----|--------------|
| Exact verification | Yes | Yes |
| State revert | Yes | Yes |
| Verification samples | calib subset (256) | calib subset (256) |

**Status**: **IDENTICAL**

### 5. Data Split

| Aspect | T05 | T06.1 Global |
|--------|-----|--------------|
| Calib samples | 256 | 256 |
| Eval samples | 2000 | 2000 |
| Disjoint? | Likely yes | Likely yes |

**Status**: **SIMILAR** (assuming T05 uses same split)

### 6. Stopping Rule

| Aspect | T05 | T06.1 Global |
|--------|-----|--------------|
| Budget | 50 physical flips | 50 physical flips |
| Early stop | No | No |

**Status**: **IDENTICAL**

---

## Why T05 ≠ T06.1 Global (Strictly)

### Difference 1: Baseline Accuracy
- T05: 92.33%
- T06.1: 92.40%

**Impact**: 0.07% baseline difference is negligible.

### Difference 2: Proxy Screening Method
- T05: First-order Taylor (w * Δg)
- T06.1: Per-layer \|grad\| ranking

**Impact**: May cause different candidate ordering before exact verification.

### Difference 3: Metadata in Candidate Pool
- T05: Includes metadata candidates (even if never chosen)
- T06.1: Weight candidates only

**Impact**: T05's exact verification compares against metadata candidates too.

---

## Why T05 ≈ T06.1 Global (Practically)

### Similarity 1: Same Effective Attack Type
- Both attack weight bits exclusively
- Both use sparse-gated forward semantics
- Both use exact verification

### Similarity 2: Close Final Accuracy
- T05: 9.96%
- T06.1: 92.40% → 10.20%
- Difference: 0.24% (within eval noise)

### Similarity 3: Same Budget and Exact Verification
- 50 physical flips
- K=64 exact verification
- 256 calib samples, 2000 eval samples

---

## Why T05 Chose 100% Weight_Bit

### Hypothesis 1: Weight-Bit is More Damaging
Evidence from T05 log:
```
Step 1: weight_bit bit7@w[287] | exact_L=0.3623
```
Top weight-bit candidates consistently outperform metadata candidates in exact loss increase.

### Hypothesis 2: Metadata Constraints Reduce Effectiveness
- **Index 1-bit**: Limited to Hamming distance ≤ 1 transitions
- **Bitmask swap**: Cost-2 constraint (maintains popcount=2)
- **Weight-bit**: No constraint (any bit, any weight)

### Hypothesis 3: Gradient Magnitude Favors Weights
Metadata attacks rely on sparse structure, which has fewer degrees of freedom than dense weight bits.

---

## Strict Equivalence Conditions

For T05 ≡ T06.1 global, ALL of the following must hold:

| Condition | Current Status | Required Change |
|-----------|----------------|-----------------|
| 1. Same attack surface | ❌ T05 includes metadata | Restrict T05 to weight_bit only |
| 2. Same proxy screening | ❌ Different methods | Unify to one formula |
| 3. Same candidate pool | ❌ T05 has metadata pool | Remove metadata from T05 |
| 4. Same exact verification | ✅ Both use exact | N/A |
| 5. Same data split | ⚠️ Assumed, not verified | Verify disjoint calib/eval |
| 6. Same baseline | ⚠️ 92.33% vs 92.40% | Use same checkpoint load |
| 7. Same deterministic order | ❌ Different ranking | Use same sorting criteria |

---

## Minimal Alignment Experiment

To verify T05 ≈ T06.1 alignment:

```python
# Modify T05 to:
# 1. enabled_actions: weight_bit only (remove metadata)
# 2. Use same proxy formula as T06.1 (|grad|)
# 3. Use same checkpoint loading path
# 4. Verify calib/eval are disjoint

# Expected: Final acc within ±0.5% of T06.1 global
```

---

## Conclusions

### Q1: Should T05 and T06.1 global be identical?
**Answer**: No, not with current implementations. Protocol differences exist.

### Q2: Why are they close (9.96% vs 10.20%)?
**Answer**: Both effectively perform weight-bit attacks with exact verification. Metadata candidates in T05 are consistently outcompeted.

### Q3: Is the difference a bug?
**Answer**: No. The difference (0.24%) is within expected variance for:
- Different baseline accuracy (0.07%)
- Different proxy screening methods
- Eval noise on 2000 samples

### Q4: How to make them identical?
**Answer**: Modify T05 to:
1. Remove metadata actions (enabled_actions=['weight_bit'])
2. Use same proxy screening as T06.1
3. Use same checkpoint loading

---

## Recommendations

### For Current Analysis
1. **Accept T05 and T06.1 as effectively equivalent** (0.24% difference is negligible)
2. **Report T05's 100% weight_bit selection** as a key finding
3. **Note that metadata attacks are outcompeted** by weight-bit in joint selection

### For Future Work
1. **Run T05 with metadata-only** to measure standalone effectiveness
2. **Analyze why weight-bit outperforms metadata** (gradient analysis)
3. **Consider constrained joint attack** (e.g., force 50% metadata selection)
